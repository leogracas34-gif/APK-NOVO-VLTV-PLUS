// Arquivo de configuração de build de nível superior.
// Aqui definimos os plugins que serão aplicados aos módulos do projeto.

plugins {
    id("com.android.application") version "8.2.0" apply false
    id("org.jetbrains.kotlin.android") version "1.9.0" apply false
}
